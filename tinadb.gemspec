# frozen_string_literal: true

Gem::Specification.new do |gem|
  gem.name                        = 'tinadb'
  gem.version                     = '0.0.1'
  gem.license                     = 'MIT'
  gem.summary                     = "tinadb is a database but it's not"

  gem.author                      = 'diogoleitao'
  gem.email                       = 'diogo.n.leitao+tinadb@gmail.com'

  gem.files                       = ['Gemfile', 'lib/tinadb.rb']
  gem.require_paths               = ['lib']

  gem.metadata['source_code_uri'] = 'https://github.com/diogoleitao/tinadb'
end
